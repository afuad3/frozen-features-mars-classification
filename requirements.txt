# Frozen foundation-model features for Mars image classification
# Pinned for Python 3.9.x on macOS arm64 (Apple Silicon / MPS). No CUDA.
# NOTE: these are best-effort pins. The ACTUAL resolved versions are captured by
# scripts/00_environment.py -> results/environment.md (pip freeze) after install.

# --- Core numerical stack ---
numpy==2.0.2
scipy==1.13.1

# --- Deep learning (MPS-enabled wheels; installed from default PyPI index) ---
torch==2.4.1
torchvision==0.19.1

# --- Frozen feature extractors ---
timm==1.0.9              # ResNet-50 weights + transforms (cross-check)
transformers==4.44.2     # DINOv2 (facebook/dinov2-base), CLIP (openai/clip-vit-base-patch16)
safetensors==0.4.5
huggingface-hub==0.24.6

# --- Classifier + metrics ---
scikit-learn==1.5.2

# --- Dimensionality reduction / visualization ---
umap-learn==0.5.6
numba==0.60.0
matplotlib==3.9.4
seaborn==0.13.2

# --- Data IO / tables ---
pandas==2.2.2
pyarrow==17.0.0
h5py==3.11.0
pyyaml==6.0.2

# --- Utilities ---
tabulate==0.9.0          # pandas.to_markdown() for audit/report tables
tqdm==4.66.5
requests==2.32.3
pillow>=10.4.0
